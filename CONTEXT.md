# AAOCA Text Data Processing

# Project Context

这是复旦大学 ZMIC Lab 参与的一个小儿先天性心脏病真实世界多模态临床数据项目。当前我们这个小组只处理其中 AAOCA 异常主动脉起源冠状动脉亚组。合作医院已经提供了一批真实患者数据，规模约 200+ cases。我们需要开展后续研究，较明确的方向包括：

1. 利用 CTA 等影像进行 AAOCA 结构分析（这个我不需要做）；
2. 预测需要手术治疗和保守治疗（后面再说）；
3. 后续如果获得随访/预后数据，可能进一步研究术后恢复和预后；
4. 最终结合文本与影像进行多模态建模。

但具体 prediction target、clinical endpoint 等还不确定。

目前最重要的工作是：

> 系统理解真实数据，建立可复用的数据处理流程，并明确现有数据究竟能支持哪些临床任务。

---

## 1. 我的分工

我负责 **文本数据**。

另有同学主要处理 CTA / 超声等影像数据。

我当前的任务还没到要训练预测模型，只需要：

> 将医院导出的原始文本病例数据整理成可计算、可追溯、可复用的数据结构。

之后文本侧结果可能与影像数据合并，用于多模态实验。

---

## 2. 当前已有数据

已有清洗后的患者列表：

`data/sample_list/AAOCA_患者病例_清洗后.csv`

文本资料 **全部是从医院网页系统导出的、排版是否混乱的 PDF**

暂时不假设需要 OCR，先考虑 PDF 内部 text layer。
只有确认某些 PDF 无法直接提取文字后，才考虑引入 OCR，这里我打算用本地部署的多模态 LLM 来处理，暂时把确定需要 OCR 的那部分搁置，并做好标记。

病例文书和检验结果是混杂存在的，命名分别为“xxx-病历文书 - 360视图 v2.1 Beta”“xxx-检验”。后续必须要识别：

- 病历文书；
- 检验结果；
- 手术记录；
- 出院记录；
- 病程记录；
- 门诊记录；
- 其他类型。

---

## 3. 当前研究任务中的标签问题

临床团队目前关注患者实际上接受了什么治疗：

- surgery
- conservative
- unknown

如果要建立字段，可以称为：

`observed_management`

---

## 4. 时间泄漏问题

未来如果建立：

> 根据术前信息预测患者最终是否接受手术

那么术后资料不能作为模型输入。

例如下列文本会直接泄漏答案：

- 手术记录；
- “术后第 X 天”；
- “患者昨日行……术”；
- ICU 术后记录；
- 出院小结中的手术经过。

因此从病例第一天开始，就应该尽量保留：

- document timestamp；
- document type；
- 原始来源；
- 患者；
- 文档在诊疗过程中的大致阶段。

但现阶段不要为了构造完美的 clinical stage ontology 而阻碍基础数据审计。

---

## 5. 数据处理原则

### 原始数据不可修改

### 所有结果可追溯

任何结构化字段最终都应该能够追溯到：

- patient；
- source PDF；
- page；
- original text / evidence。

### 不要早丢数据

目前不能因为：

- 非 AAOCA；
- 没做手术；
- 数据缺失；
- 暂时看起来无用；

就直接删除患者。

先标记，再由后续实验决定 cohort inclusion。

### 不追求“大而全”

当前不要构建：

- 完整医疗知识图谱；
- 全量 FHIR；
- 数百个医学字段；
- 通用 EHR platform。

目标是围绕当前真实数据建立 **minimal reusable pipeline**。

---

## 6. 当前阶段目标

短期目标不是完成医学研究。

短期目标是建立：

Raw PDF
→ 文件/患者对应关系
→ PDF 文本可靠提取
→ 基础文档结构识别
→ patient-level timeline
→ 可追溯的结构化数据
→ 后续临床字段抽取

并最终回答：

> 这批真实 AAOCA 文本数据实际上包含什么？
> 数据质量如何？
> 哪些字段可以可靠获得？
> 哪些病例可以支持哪些研究任务？
