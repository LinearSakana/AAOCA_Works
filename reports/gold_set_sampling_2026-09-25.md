# Gold Set 抽样收据（2026-09-25）

从完整 `data/derived/v0.1_full/` 运行抽样；该运行 `status=complete`、源码运行期间未变、输入 SHA256 复核通过。命令使用 `stratified_page_v1`、固定 seed `20260925`、每层 12 页。生成在受限的 `data/gold_sets/aao_pdf_pages_v1/`；`sample_id=81f579c39633ed326ca5`。仓库仅记录匿名聚合，不提交逐页清单和病例内容。

| 分层 | PDF 页数 | 常规页 | 困难覆盖页 |
|---|---:|---:|---:|
| native 病历 | 12 | 6 | 6 |
| native 检验 | 12 | 6 | 6 |
| OCR 病历 | 12 | 6 | 6 |
| OCR 检验 | 12 | 6 | 6 |
| **合计** | **48** | **24** | **24** |

样本来自 **42 份不同 PDF、41 名伪名化患者**；每位患者和每份 PDF 至多贡献两页。根据抽样时 pipeline 的候选标签（尚未经人工确认）：16 页有多个 section 起点，6 页处于跨页 section，23 页有未知/不确定日期，21 页有日期冲突或待复核提示，11 页有低置信 OCR，10 页有页外文字，2 页 normalized text 为空。类型候选包括检验、病程、入院、出院、操作、会诊、门诊、行政和非临床内容。候选标签只是**覆盖设计**，不是真实错误数。

该 48 页样本刻意富集困难情况，并保留每层随机常规对照；**不能直接推断全体 13,485 页的流行率或总体准确率**。当前没有人工核验完成页，没有任何真实 Gold Set 评分。下一步按 `docs/gold_set_review.md` 对照 PDF 标注、冻结 `r1`，再按 `docs/gold_set_evaluation.md` 对当前与后续 parser 运行评分。
